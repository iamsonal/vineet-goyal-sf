{
    "name": "core-build",
    "version": "0.1.10",
    "description": "Core build and Lightning Platform specific code for Lightning Data Service",
    "private": true,
    "scripts": {
        "prepare": "yarn build",
        "build": "rollup --config rollup.config.js",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "@salesforce/lds-adapters-apex": "0.1.3",
        "@salesforce/lds-adapters-uiapi": "0.1.3"
    }
}
