{
    "name": "core-build",
    "version": "0.1.12",
    "description": "Core build and Lightning Platform specific code for Lightning Data Service",
    "private": true,
    "scripts": {
        "prepare": "yarn build",
        "build": "rollup --config rollup.config.js",
        "clean": "rm -rf dist",
        "test": "jest",
        "test:debug": "node --inspect-brk ../../node_modules/.bin/jest --runInBand"
    },
    "devDependencies": {
        "@salesforce/lds-adapters-apex": "0.1.12",
        "@salesforce/lds-adapters-uiapi": "0.1.12"
    }
}
