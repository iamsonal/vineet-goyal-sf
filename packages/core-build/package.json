{
    "name": "core-build",
    "version": "0.1.10",
    "description": "Core build and Lightning Platform specific code for Lightning Data Service",
    "scripts": {
        "prepare": "yarn build",
        "build": "rollup --config rollup.config.js",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "@salesforce-lds-api/apex": "0.1.3",
        "@salesforce-lds-api/uiapi-records": "0.1.3"
    }
}
