// TODO: once webview is using native-side durable store implementation this file
// will instantiate LDS with that durable store, for now though we can copy the
// browser setup
export * from './lds-setup';
