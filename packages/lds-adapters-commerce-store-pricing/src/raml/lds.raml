#%RAML 1.0 Overlay
extends: ./api.raml
uses:
  lds: lds://annotations.raml

(lds.keyPrefix): 'Commerce::'

types:
  ProductPriceRepresentation:
    (lds.opaque): true

/commerce/webstores/{webstoreId}/pricing/products/{productId}:
  get:
    (lds.adapter):
      name: getProductPrice
