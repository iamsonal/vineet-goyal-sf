#%RAML 1.0 Overlay
extends: ./api.raml

uses:
  luvio: luvio://annotations.raml

(luvio.keyPrefix): 'CommunitiesSeo::'

types:
  RecordSeoPropertiesRepresentation:
    (luvio.ttl): 3600000
    (luvio.opaque): true

/connect/communities/{communityId}/seo/properties/{recordId}:
  get:
    (luvio.adapter):
      name: getRecordSeoProperties
