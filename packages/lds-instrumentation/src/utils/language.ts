const { keys } = Object;
const { isArray } = Array;
const { stringify } = JSON;

export {
    // Object
    keys as ObjectKeys,
    // Array
    isArray as ArrayIsArray,
    // JSON
    stringify as JSONStringify,
};
