const { create } = Object;
const { stringify, parse } = JSON;
const { isArray } = Array;

export {
    // Object
    create as ObjectCreate,
    // JSON
    stringify as JSONStringify,
    parse as JSONParse,
    isArray as IsArray,
};
