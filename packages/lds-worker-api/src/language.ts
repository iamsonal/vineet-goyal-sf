const { create, keys } = Object;
const { stringify, parse } = JSON;
const { isArray } = Array;

export {
    // Object
    create as ObjectCreate,
    keys as ObjectKeys,
    // JSON
    stringify as JSONStringify,
    parse as JSONParse,
    isArray as IsArray,
};
