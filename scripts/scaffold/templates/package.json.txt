{
    "name": "{{PACKAGE_NAMESPACE}}/{{PACKAGE_NAME_TAG}}",
    "version": "0.1.0",
    "description": "{{DESCRIPTION_TAG}}",
    "license": "MIT",
    "main": "dist/umd/es2018/adapters.js",
    "module": "dist/es/es2018/adapters.js",
    "types": "dist/es/es2018/adapters.d.ts",
    "contributors": {{PACKAGE_CONTRIBUTORS}},
    "scripts": {
        "clean": "rm -rf dist sfdc src/generated karma/dist",
        "build": "yarn build:raml && yarn build:services && yarn build:karma",
        "build:karma": "rollup --config rollup.config.karma.js",
        "build:raml": "lds generate src/raml/lds.raml src/generated -p '../lds-compiler-plugins'",
        "build:services": "rollup --config rollup.config.js",
        "test:unit": "jest",
        "start": "karma start",
        "test": "karma start --single-run",
        "test:compat": "karma start --single-run --compat",
        "release:core": "../../scripts/release/core.js --adapter={{PACKAGE_NAME_TAG}}"
    },
    "devDependencies": {
        "@ldsjs/cli": "^{{LDS_VERSION}}",
        "@ldsjs/compiler": "^{{LDS_VERSION}}",
        "@ldsjs/engine": "^{{LDS_VERSION}}",
        "@ldsjs/lwc-lds": "^{{LDS_VERSION}}"
    }
}
